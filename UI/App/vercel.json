{
    "version": 2,
    "builds": [
      {
        "src": "App.py",
        "use": "@vercel/python",
        "config": {
          "buildCommand": "source myenv/bin/activate && python -m pip install --upgrade pip setuptools wheel"
        }
      }
    ]
  }